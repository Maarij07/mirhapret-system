<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- Color Palette - Black & White Minimalist -->
    <Color x:Key="ColorBlack">#000000</Color>
    <Color x:Key="ColorDark">#1a1a1a</Color>
    <Color x:Key="ColorCharcoal">#333333</Color>
    <Color x:Key="ColorMidGray">#666666</Color>
    <Color x:Key="ColorLightGray">#999999</Color>
    <Color x:Key="ColorBorder">#e0e0e0</Color>
    <Color x:Key="ColorOffWhite">#f5f5f5</Color>
    <Color x:Key="ColorWhite">#ffffff</Color>

    <!-- Status Colors -->
    <Color x:Key="StatusPending">#999999</Color>
    <Color x:Key="StatusConfirmed">#1a56db</Color>
    <Color x:Key="StatusProcessing">#f5a623</Color>
    <Color x:Key="StatusShipped">#1a7a4a</Color>
    <Color x:Key="StatusDelivered">#000000</Color>
    <Color x:Key="StatusCancelled">#c0392b</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="BrushBlack" Color="{StaticResource ColorBlack}"/>
    <SolidColorBrush x:Key="BrushDark" Color="{StaticResource ColorDark}"/>
    <SolidColorBrush x:Key="BrushCharcoal" Color="{StaticResource ColorCharcoal}"/>
    <SolidColorBrush x:Key="BrushMidGray" Color="{StaticResource ColorMidGray}"/>
    <SolidColorBrush x:Key="BrushLightGray" Color="{StaticResource ColorLightGray}"/>
    <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource ColorBorder}"/>
    <SolidColorBrush x:Key="BrushOffWhite" Color="{StaticResource ColorOffWhite}"/>
    <SolidColorBrush x:Key="BrushWhite" Color="{StaticResource ColorWhite}"/>

    <!-- Status Brushes -->
    <SolidColorBrush x:Key="BrushStatusPending" Color="{StaticResource StatusPending}"/>
    <SolidColorBrush x:Key="BrushStatusConfirmed" Color="{StaticResource StatusConfirmed}"/>
    <SolidColorBrush x:Key="BrushStatusProcessing" Color="{StaticResource StatusProcessing}"/>
    <SolidColorBrush x:Key="BrushStatusShipped" Color="{StaticResource StatusShipped}"/>
    <SolidColorBrush x:Key="BrushStatusDelivered" Color="{StaticResource StatusDelivered}"/>
    <SolidColorBrush x:Key="BrushStatusCancelled" Color="{StaticResource StatusCancelled}"/>

    <!-- Typography -->
    <FontFamily x:Key="FontFamilyDefault">Segoe UI, Arial, sans-serif</FontFamily>
    <FontFamily x:Key="FontFamilyMono">Consolas, Courier New, monospace</FontFamily>

    <!-- Font Sizes -->
    <sys:Double x:Key="FontSizeXS">12</sys:Double>
    <sys:Double x:Key="FontSizeSM">13</sys:Double>
    <sys:Double x:Key="FontSizeBase">14</sys:Double>
    <sys:Double x:Key="FontSizeLG">16</sys:Double>
    <sys:Double x:Key="FontSizeXL">20</sys:Double>
    <sys:Double x:Key="FontSize2XL">24</sys:Double>
    <sys:Double x:Key="FontSize4XL">32</sys:Double>

    <!-- Spacing -->
    <sys:Double x:Key="Spacing4">4</sys:Double>
    <sys:Double x:Key="Spacing8">8</sys:Double>
    <sys:Double x:Key="Spacing12">12</sys:Double>
    <sys:Double x:Key="Spacing16">16</sys:Double>
    <sys:Double x:Key="Spacing20">20</sys:Double>
    <sys:Double x:Key="Spacing24">24</sys:Double>
    <sys:Double x:Key="Spacing32">32</sys:Double>
    <sys:Double x:Key="Spacing48">48</sys:Double>

    <!-- Button Styles -->
    <Style x:Key="ButtonPrimary" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BrushBlack}"/>
        <Setter Property="Foreground" Value="{StaticResource BrushWhite}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushBlack}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="FontFamily" Value="{StaticResource FontFamilyDefault}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeSM}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BrushCharcoal}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource BrushDark}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource BrushBorder}"/>
                <Setter Property="Foreground" Value="{StaticResource BrushLightGray}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ButtonSecondary" TargetType="Button" BasedOn="{StaticResource ButtonPrimary}">
        <Setter Property="Background" Value="{StaticResource BrushWhite}"/>
        <Setter Property="Foreground" Value="{StaticResource BrushBlack}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushBlack}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BrushOffWhite}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ButtonDanger" TargetType="Button" BasedOn="{StaticResource ButtonPrimary}">
        <Setter Property="Background" Value="{StaticResource BrushStatusCancelled}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushStatusCancelled}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#a02d23"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- TextBox Style -->
    <Style x:Key="TextBoxDefault" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource BrushWhite}"/>
        <Setter Property="Foreground" Value="{StaticResource BrushCharcoal}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,9"/>
        <Setter Property="FontFamily" Value="{StaticResource FontFamilyDefault}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeBase}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource BrushBlack}"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Label Style -->
    <Style x:Key="LabelDefault" TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource BrushCharcoal}"/>
        <Setter Property="FontFamily" Value="{StaticResource FontFamilyDefault}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeXS}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="0,0,0,6"/>
    </Style>

</ResourceDictionary>
